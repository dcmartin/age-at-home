---
declared-services:
  visual-insights-service:
    label: visual_insights
    plan: experimental
applications:
- services:
  - visual-insights-service
  name: age-at-home
  command: node app.js
  path: .
  memory: 512M
  env:
    NODE_ENV: production
